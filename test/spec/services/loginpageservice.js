/*
'use strict';

describe('Service: loginpageservice', function () {

  // load the service's module
  beforeEach(module('myAssignmentTaskApp'));

  // instantiate service
  var loginpageservice;
  beforeEach(inject(function (_loginpageservice_) {
    loginpageservice = _loginpageservice_;
  }));

  it('should do something', function () {
    expect(!!loginpageservice).toBe(true);
  });

});
*/
